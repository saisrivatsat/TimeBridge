{
  "name": "timebridge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Find humane meeting and calling times across time zones.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/app.js && node --check src/explorer.js && node --check src/time.js",
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
